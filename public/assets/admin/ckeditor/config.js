CKEDITOR.editorConfig = function( config ) {
    // Allow script tags
    config.extraAllowedContent = 'script[*]';

    // Optionally, you can also disable Advanced Content Filter (ACF) entirely, but this is not recommended for security reasons.
    // config.allowedContent = true;
};
